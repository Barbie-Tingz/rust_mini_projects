/* Meet the Employees & Clients

**The bit:** The agency finally needs actual records instead of vibes — time to define who
its employees and clients actually are.

**Your mission:** Define structs for your core character types, so later chapters have
something to build on.*/

// 1. Define an Employee struct with a few fields (e.g. name, job_title, sanity_level).
// 2. Define a Client struct with a few fields (e.g. name, case_description, weirdness_rating).
// 3. Create one instance of each struct inside main with example data.
// 4. Write a function that takes a reference to an Employee and prints a short introduction.
// 5. Write a function that takes a reference to a Client and prints a short introduction.
// 6. Call both functions from main with your sample instances.
// 7. (Optional) Add an impl block for Employee with a method that returns a greeting String,
//    instead of using a standalone function.